var path= require('path');
var dataCollect = require('../routing/apiRoutes');

function totalDifference(){
    //need to take the friendsData.scores and turn it into an array, 
    //need to then check it number for number against the newfriendscores array, 
    //the "friend" 
}